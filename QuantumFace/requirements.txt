# QuantumFace — research/education prototype dependencies
# All quantum work runs on local simulators; no quantum hardware required.

# --- quantum ---
pennylane>=0.42          # differentiable state-vector + density-matrix simulators

# --- classical ML / baseline ---
torch==2.2.2             # CPU build; pin avoids CUDA-only wheels on some platforms
torchvision==0.17.2      # must match torch 2.2.2
facenet-pytorch==2.6.0   # pretrained FaceNet (InceptionResnetV1) + MTCNN
scikit-learn>=1.3        # PCA, SVM/kNN, LFW loader, metrics
opencv-python-headless==4.9.0.80  # pinned: 4.13+ requires numpy>=2 (breaks torch 2.2.2)
numpy>=1.24,<2           # torch 2.2.2 + opencv 4.9 are built against numpy<2
pandas>=2.0
matplotlib>=3.6
pillow>=10.2
tqdm

# --- storage / security ---
kyber-py>=1.0            # pure-python ML-KEM (FIPS 203) — Kyber KEM
cryptography>=42.0       # AES-GCM symmetric cipher for the KEM-wrapped payload

# --- api / frontend ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9  # file uploads
streamlit>=1.32
httpx>=0.27              # test client

# --- testing ---
pytest>=8.0

# --- optional: experimental real-QPU path (NOT needed for simulators) ---
# Install only if you want to run on IBM Quantum hardware (opt-in, needs your token):
#   pip install pennylane-qiskit qiskit-ibm-runtime
